
description = "Pro Spring 7: Spring Boot Projects"

subprojects {
    group = "com.apress.prospring7.boot"

    apply(plugin = "java")
    apply(plugin = "io.spring.dependency-management")
    apply(plugin = "org.springframework.boot")
}
